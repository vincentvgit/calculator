# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - PocketCalculator

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://registrevincentvgit.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_ae1c406a94164b02938dc69e42317107 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_de7cdaf442ef442998407088ee8436da }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: registrevincentvgit.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_ae1c406a94164b02938dc69e42317107 }}/registrevincentvgit/registrevincentvgit/calculator:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'PocketCalculator'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_4e5dab06ff5b4dc6849fd626ea0b4ba7 }}
        images: 'registrevincentvgit.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_ae1c406a94164b02938dc69e42317107 }}/registrevincentvgit/registrevincentvgit/calculator:${{ github.sha }}'